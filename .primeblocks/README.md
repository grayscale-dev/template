# PrimeBlocks Fallback Assets

Primary local library path is configured via
`site-remodel.config.json -> primeBlocksLocalPath` (default
`../components`).

Use this `.primeblocks/` folder only as a fallback location for
additional licensed snippets that are not yet present in the main
library.

Do not commit licensed source files to public repositories.
